# vim: et
---

- name: Apply inssserver config files
  ansible.builtin.copy:
    src: "{{ item.source }}"
    dest: "{{ item.dest }}"
    mode: '0644'
    owner: inssserver
    group: inssserver
  loop: "{{ inssserver_files }}"

- name: Apply inssserver template files
  ansible.builtin.template:
    src: "{{ item.source }}"
    dest: "{{ item.dest }}"
    mode: '0644'
    owner: inssserver
    group: inssserver
  loop: "{{ inssserver_templates }}"
