# vim: et
---

- name: Install Intel graphics drivers (Fedora)
  become: true
  become_user: root
  ansible.builtin.package:
    name: "{{ item }}"
    state: present
  loop: "{{ intel_graphics_drivers_fedora }}"
